    for (let i = 0; i < str.length; i++) {
        if (str[i] != '9') {
            first += str[i]
            break
    let str = num.toString()
    let first = ''
var minMaxDifference = function (num) {
 */
 * @return {number}
 * @param {number} num
/**
        }
