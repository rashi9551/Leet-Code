var countSeniors = function (details) {
    for (let str of details) {
        if (Number(str[11] + str[12]) > 60) {
    let res = 0
            res++
        }
    }
 */
 * @return {number}
/**
 * @param {string[]} details
