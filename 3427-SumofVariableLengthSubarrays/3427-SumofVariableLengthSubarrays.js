        for (let j = start; j <= i; j++) {
            result += nums[j];
        const start = Math.max(0, i - nums[i]);
    for (let i = 0; i < nums.length; i++) {
        }
    }

    return result;
};
