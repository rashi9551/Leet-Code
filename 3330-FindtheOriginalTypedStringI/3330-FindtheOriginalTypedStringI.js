        if (word[i] === word[i + 1]) {
    }
            count++
        }
    return count + 1
};
    for (let i = 0; i < word.length; i++) {
    count = 0
var possibleStringCount = function (word) {
 * @return {number}
 */
 * @param {string} word
/**
