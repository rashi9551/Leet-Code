            arr[i]-=1
            count++
        }
        if(arr[i]!=0){
    {
    let count=0
    while(arr[k]!=0)
    let i=0
var timeRequiredToBuy = function(arr, k) {
 */
 * @return {number}
        i===arr.length-1?i=0:i++
/**
 * @param {number[]} tickets
 * @param {number} k
    }
    return count
};
[
