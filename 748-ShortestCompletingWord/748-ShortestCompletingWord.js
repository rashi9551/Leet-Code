                isValid = false;
            if ((wordCount.get(char) || 0) < count) {
        for (let [char, count] of targetCount) {

        let isValid = true;
        const wordCount = getCharCount(word);
    for (let word of words) {

    let result = "";
    const targetCount = getCharCount(licensePlate.replace(/[^a-z]/gi, '').toLowerCase());
var shortestCompletingWord = function (licensePlate, words) {
 */
 * @return {string}
                break;
            }
        }
