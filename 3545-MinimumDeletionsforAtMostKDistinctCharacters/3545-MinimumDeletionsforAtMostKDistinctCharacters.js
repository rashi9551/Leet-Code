        if (l <= k) {
            return res
        }
        l--
        res += occ
    }
    for (let [key, occ] of freq) {
    let res = 0
    let l = freq.length
    let freq = [...map.entries()].sort((a, b) => a[1] - b[1])
    }
        map.set(s[i], (map.get(s[i]) || 0) + 1)
