        console.log(l," leng ",occ," occ ", k," limit " , res ,"res")
        if(l<=k){
            return res
    for(let [key,occ] of freq){
    let res = 0
    let l=freq.length
    let freq = [...map.entries()].sort((a,b)=>a[1]-b[1])
    }
        map.set(s[i],(map.get(s[i])||0)+1)
    for (let i = 0; i < s.length; i++) {
    let map = new Map()
var minDeletion = function (s, k) {
 */
