        }
    }
    return maxDiff
};
                let val = nums[j] - nums[i]
                maxDiff = Math.max(maxDiff, val)
            }
    for (let i = 0; i < nums.length; i++) {
        for (let j = i + 1; j < nums.length; j++) {
            if (nums[i] < nums[j]) {
 */
var maximumDifference = function (nums) {
    let maxDiff = -1
 * @param {number[]} nums
 * @return {number}
/**
