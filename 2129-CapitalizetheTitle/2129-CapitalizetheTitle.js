    {
    return arr.map(word => 
    let arr=title.toLowerCase().split(' ')
var capitalizeTitle = function(title) {
 */
 * @return {string}
 * @param {string} title
        if(word.length<=2){
            return word
